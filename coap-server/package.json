{
  "name": "coap-server",
  "version": "1.0.0",
  "description": "CoAP Server to recieve CoAP Request from beaglebones and create HTTP request with the CoAP request payload to the SiloAPI.  ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker": "nodemon -L ."
  },
  "author": "Cesar Jeanroy",
  "license": "ISC",
  "dependencies": {
    "coap": "^0.21.0",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "nodemon": "^1.17.1"
  }
}
